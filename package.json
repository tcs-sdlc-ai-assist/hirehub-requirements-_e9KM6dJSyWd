{
  "name": "hirehub-onboarding-portal",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "prop-types": "^15.8.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.4.0"
  }
}
Created package.json for Vite + React + JavaScript project with dependencies (react, react-dom, prop-types) and devDependencies (@vitejs/plugin-react, vite). Includes dev, build, preview scripts. Ready to run with npm install then npm run dev.